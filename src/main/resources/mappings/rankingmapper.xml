<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="RankDao">
    <insert id="insertRanking" parameterType="Rank">
        INSERT INTO RANKING (RANK_ID, DATE, TIME, GAME, COMMENT)
        VALUES (#{rank_id}, #{playdate}, #{playtime}, #{gametype}, #{comment})
    </insert>
</mapper>